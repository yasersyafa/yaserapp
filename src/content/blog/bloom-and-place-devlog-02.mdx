---
title: "Bloom & Place devlog #2: the baseline I'm trying to beat"
dek: Before refactoring anything, I measured the prototype's coupling. Here are the numbers I have to improve on — including the line of code I'm most embarrassed by.
date: 2026-05-02
tags: [unity, architecture, metrics]
readingTime: 5
---

## Don't refactor what you didn't measure

Last post I committed to a falsifiable architecture claim. That only works if the "before" picture is real — not a strawman I cooked up to make the after look good. So before touching VContainer, I ran Ce/Ca over the existing prototype as it stands today.

## The headline numbers

| Class | Ce | Ca | I | Notes |
|---|---|---|---|---|
| `Plant` | 5 | — | — | depends on `Seat`, `PlantPreference`, `PlantOutlineHover`, `PlantSelectionManager`, `CustomCursorManager` |
| `ScoreManager` | — | — | 0.75 | near the unstable end of the scale |
| `Seat` | — | — | — | calls `PlantSelectionManager.Instance` directly from `OnMouseDown()` |

`Plant` has Ce = 5. Five separate concrete classes it knows about by name. That's the symptom — every time I touched plant behaviour I was implicitly touching five other systems' assumptions.

`ScoreManager`'s instability is 0.75. Not catastrophic on paper, but for a class that everyone's supposed to depend *on* (it's a sink — score data flows toward it), I want it closer to 0. Today it's reaching outward more than it's being reached inward, which is the wrong shape for that role.

## The line I'm most embarrassed by

```csharp
// Seat.cs
void OnMouseDown() {
  PlantSelectionManager.Instance.OnSeatClicked(this);
}
```

`Seat` is core gameplay state. `PlantSelectionManager` is, well, a manager. The dependency arrow points the wrong direction — core is reaching into the manager layer. This violates the Stable Dependency Principle in the most literal way possible: an unstable handler in a stable class.

The reason it's there is the reason these things are always there: I needed selection state from inside an input callback, and `.Instance` was the closest thing in arm's reach.

## Why I'm publishing this before the fix

Two reasons.

One: if I publish only the after-numbers, you have no way to evaluate the claim. "We made it better" with no baseline is marketing.

Two: writing the baseline down forces me to commit to it. The Ce/Ca for `Plant` won't quietly drift while I work — it's pinned to this post. If the refactor doesn't move it, that's on the architecture, not on me re-running the tool with friendlier inputs.

## What I want the after-table to look like

I'm not picking exact targets yet — that's the kind of thing you cherry-pick toward. The qualitative goals:

- `Plant` Ce drops by depending on **interfaces** wired by VContainer, not concrete classes scattered across the project.
- `ScoreManager` becomes a passive sink — other systems publish `ScoreChanged` events to it, it doesn't reach out.
- The `Seat.OnMouseDown` line stops calling any `.Instance` at all. Selection becomes an event the seat publishes, not a method it calls on a manager.

## Next

Devlog #3 zooms out: what coupling actually is, why a metric from 1968 still earns its keep, and why "loosely coupled" is harder to live up to than to say.
